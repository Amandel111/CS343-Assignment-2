package main
package mapreduce
type word String
// Question: is parsing the doc done outside a function?
type word {
	string word
}
func (t *word) map(args *word, )

func main() {
	// TODO: add argument processing and run both single-threaded and multi-threaded functions
}